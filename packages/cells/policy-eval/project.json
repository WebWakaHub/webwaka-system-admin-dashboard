{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "cell-policy-eval",
  "sourceRoot": "packages/cells/policy-eval/src",
  "projectType": "library",
  "tags": [
    "layer:cell",
    "type:capability"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/cells/policy-eval",
        "tsConfig": "packages/cells/policy-eval/tsconfig.json",
        "main": "packages/cells/policy-eval/src/index.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/cells/policy-eval/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
