{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "organ-in-log-aggregation",
  "sourceRoot": "packages/organs/in-log-aggregation/src",
  "projectType": "library",
  "tags": [
    "layer:organ",
    "domain:instrumentation",
    "type:business"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/organs/in-log-aggregation",
        "tsConfig": "packages/organs/in-log-aggregation/tsconfig.json",
        "main": "packages/organs/in-log-aggregation/src/index.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/organs/in-log-aggregation/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
