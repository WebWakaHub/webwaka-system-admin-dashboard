{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "organ-trn-shipment-tracking",
  "sourceRoot": "packages/organs/trn-shipment-tracking/src",
  "projectType": "library",
  "tags": [
    "layer:organ",
    "domain:transport",
    "type:business"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/organs/trn-shipment-tracking",
        "tsConfig": "packages/organs/trn-shipment-tracking/tsconfig.json",
        "main": "packages/organs/trn-shipment-tracking/src/index.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/organs/trn-shipment-tracking/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
