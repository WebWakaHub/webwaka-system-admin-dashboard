{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "organelle-validation-engine",
  "sourceRoot": "packages/organelles/validation-engine/src",
  "projectType": "library",
  "tags": [
    "layer:organelle",
    "scope:validation-engine",
    "type:primitive"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/organelles/validation-engine",
        "tsConfig": "packages/organelles/validation-engine/tsconfig.json",
        "main": "packages/organelles/validation-engine/src/index.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/organelles/validation-engine/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
