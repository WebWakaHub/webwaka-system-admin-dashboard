{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "organelle-workflow-orchestrator",
  "sourceRoot": "packages/organelles/workflow-orchestrator/src",
  "projectType": "library",
  "tags": [
    "layer:organelle",
    "scope:workflow-orchestrator",
    "type:primitive"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/organelles/workflow-orchestrator",
        "tsConfig": "packages/organelles/workflow-orchestrator/tsconfig.json",
        "main": "packages/organelles/workflow-orchestrator/src/index.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/organelles/workflow-orchestrator/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
