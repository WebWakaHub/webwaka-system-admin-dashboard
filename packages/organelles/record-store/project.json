{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "organelle-record-store",
  "sourceRoot": "packages/organelles/record-store/src",
  "projectType": "library",
  "tags": [
    "layer:organelle",
    "scope:record-store",
    "type:primitive"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/organelles/record-store",
        "tsConfig": "packages/organelles/record-store/tsconfig.json",
        "main": "packages/organelles/record-store/src/index.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/organelles/record-store/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
