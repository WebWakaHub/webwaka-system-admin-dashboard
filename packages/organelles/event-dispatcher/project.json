{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "organelle-event-dispatcher",
  "sourceRoot": "packages/organelles/event-dispatcher/src",
  "projectType": "library",
  "tags": [
    "layer:organelle",
    "scope:event-dispatcher",
    "type:primitive"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/organelles/event-dispatcher",
        "tsConfig": "packages/organelles/event-dispatcher/tsconfig.json",
        "main": "packages/organelles/event-dispatcher/src/index.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/organelles/event-dispatcher/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
