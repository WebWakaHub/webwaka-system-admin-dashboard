{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "organelle-message-gateway",
  "sourceRoot": "packages/organelles/message-gateway/src",
  "projectType": "library",
  "tags": [
    "layer:organelle",
    "scope:message-gateway",
    "type:primitive"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/organelles/message-gateway",
        "tsConfig": "packages/organelles/message-gateway/tsconfig.json",
        "main": "packages/organelles/message-gateway/src/index.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/organelles/message-gateway/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
