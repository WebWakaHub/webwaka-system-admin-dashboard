{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "organelle-scheduler-executor",
  "sourceRoot": "packages/organelles/scheduler-executor/src",
  "projectType": "library",
  "tags": [
    "layer:organelle",
    "scope:scheduler-executor",
    "type:primitive"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/organelles/scheduler-executor",
        "tsConfig": "packages/organelles/scheduler-executor/tsconfig.json",
        "main": "packages/organelles/scheduler-executor/src/index.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/organelles/scheduler-executor/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
