{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "organelle-boundary-context",
  "sourceRoot": "packages/organelles/boundary-context/src",
  "projectType": "library",
  "tags": [
    "layer:organelle",
    "scope:boundary-context",
    "type:primitive"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/organelles/boundary-context",
        "tsConfig": "packages/organelles/boundary-context/tsconfig.json",
        "main": "packages/organelles/boundary-context/src/index.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/organelles/boundary-context/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
