{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "system-inf-cloudplatform",
  "sourceRoot": "packages/systems/inf-cloudplatform/src",
  "projectType": "library",
  "tags": [
    "layer:system",
    "domain:infrastructure",
    "type:platform"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/systems/inf-cloudplatform",
        "tsConfig": "packages/systems/inf-cloudplatform/tsconfig.json",
        "main": "packages/systems/inf-cloudplatform/src/index.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/systems/inf-cloudplatform/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
