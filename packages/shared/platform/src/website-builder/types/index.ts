export interface WebsiteConfig { database: any; eventBus: any; }
export interface Page { id: string; siteId: string; title: string; slug: string; content: any; seoMeta: any; }
export interface Template { id: string; name: string; structure: any; }
