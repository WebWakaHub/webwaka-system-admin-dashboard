{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "tissue-monitor",
  "sourceRoot": "packages/tissues/monitor/src",
  "projectType": "library",
  "tags": [
    "layer:tissue",
    "type:coordination"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/tissues/monitor",
        "tsConfig": "packages/tissues/monitor/tsconfig.json",
        "main": "packages/tissues/monitor/src/index.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/tissues/monitor/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
