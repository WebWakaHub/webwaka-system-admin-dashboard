{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "tissue-cmd-coord",
  "sourceRoot": "packages/tissues/cmd-coord/src",
  "projectType": "library",
  "tags": [
    "layer:tissue",
    "type:coordination"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/tissues/cmd-coord",
        "tsConfig": "packages/tissues/cmd-coord/tsconfig.json",
        "main": "packages/tissues/cmd-coord/src/index.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/tissues/cmd-coord/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
